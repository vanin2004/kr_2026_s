# Алгоритм подбора репетиторов (Recommendation Engine)

Разработанный алгоритм основан на гибридном подходе: строгой фильтрации (Hard Filters) и гибком ранжировании (Soft Scoring) на основе многокритериальной оценки с учетом весов пользователя.

## 1. Этап 1: Строгие фильтры (Hard Filters)
Прежде чем применять математические вычисления ранга, система мгновенно отсекает неподходящие варианты, экономя вычислительные ресурсы.

*   **Предмет и специализация:** Точное совпадение (например, "Математика - Подготовка к ЕГЭ").
*   **Максимальная цена (`max_price`):** Ставка репетитора $\le$ `max_price` ученика.
*   **Минимальный опыт (`min_experience`):** Стаж $\ge$ `min_experience`.
*   **Верификация (`verified_only`):** Проверка документов/дипломов репетитора.
*   **Пересечение расписаний (Schedule Match):** Наличие минимум `N` часов в неделю, когда свободен и репетитор, и ученик.
*   **Обязательные теги (Required Tags):** Прямая фильтрация (например, если ученик отметил тег `native_speaker` как строгий фильтр, отсекаются все, у кого его нет).

---

## 2. Этап 2: Объективные метрики репетитора (Свойства $O_i$)
Все метрики нормализуются в диапазоне $[0, 1]$. Оценки строятся на основе Байесовского среднего, чтобы репетитор с одним отзывом 5.0 не обходил репетитора с сотней отзывов 4.9.

### $O_1$: Эффективность (Effectiveness)
Основана на реальном прогрессе студентов.
*   **Расчет:** $\frac{\text{Средний прирост баллов}}{\text{Месяцы сотрудничества}} \times \text{Коэффициент удержания}$
*   **Коэффициент удержания (LTV бонус):** $1.0 + 0.1 \times (\bar{m} - 1)$, где $\bar{m}$ — среднее количество месяцев занятий с одним учеником (ограничено максимумом в $1.5$).
*   *Нормализация:* Деление на эмпирический максимум системы (например, 20 баллов в месяц). $O_1 = \min(\text{Прирост} / \max\_прогресс, 1.0)$.

### $O_2$: Качество коммуникации (Communication Quality)
Оценка по завершенным урокам и отзывам.
*   **Расчет:** Байесовское среднее оценки за коммуникацию по 5-балльной шкале. 
*   *Нормализация:* $O_2 = \frac{\text{BayesianScore}}{5}$

### $O_3$: Уровень экспертизы (Expertise Level)
Учитывает отзывы учеников и подтвержденные квалификации на платформе.
*   **Расчет:** $0.7 \times \text{BayesianScore от учеников (норм. к 1)} + 0.3 \times \text{Бонус за сертификаты}$.

### $O_4$: Отзывчивость (Responsiveness)
Скорость ответа на заявки/сообщения.
*   **Расчет:** Оценка падает линейно или экспоненциально в зависимости от времени ответа.
*   *Нормализация:* $O_4 = \max(0, 1 - \frac{\text{Среднее время ответа (час)}}{24})$. Если ответ быстрее 1 часа, $O_4 \approx 1$.

### $O_5$: Совпадение по тегам (Tag Match / Affinity Score)
Преподаватель задает себе произвольные теги (например, `#разговорный_английский`, `#it_сфера`, `#строгий_подход`, `#гибкий_график`), а ученик указывает желаемые теги в поиске или в профиле.
*   **Расчет:** Процент совпадения желаемых тегов ученика с тегами преподавателя.
*   *Нормализация:* $O_5 = \frac{|Tags_{tutor} \cap Tags_{student}|}{|Tags_{student}|}$ (от 0 до 1). Если ученик не указал теги, $O_5 = 1.0$ (параметр не занижает общий балл).

### $O_6$: Конверсия (Заинтересованность профилем) - Опционально
*   **Расчет:** Отношение количества пробных занятий к просмотрам профиля.

---

## 3. Этап 3: Персонализация (Субъективные веса $k_i$)
Ученик при регистрации или в процессе поиска задает приоритеты (ползунком или отвечая на вопросы). 
Сумма всех коэффициентов: $\sum_{i=1}^5 k_i = 1.0$.

По умолчанию (сбалансированный профиль): 
$k_1 = 0.30$ (Эффективность), $k_2 = 0.15$ (Коммуникация), $k_3 = 0.20$ (Экспертиза), $k_4 = 0.15$ (Отзывчивость), $k_5 = 0.20$ (Совпадение по тегам).

*Если ученику нужно "просто сдать на проходной", он может снизить вес экспертизы ($k_3$) и повысить вес коммуникации ($k_2$). Если нужно срочно подготовиться к олимпиаде — приоритет отдается эффективности ($k_1$) и экспертизе ($k_3$). Если важен специфический подход или ниша, максимизируется совпадение тегов ($k_5$).*

---

## 4. Этап 4: Вычисление итогового ранга (Match Score)

Итоговый рейтинг (Score) репетитора $T$ для ученика $S$:

$$ Score(T, S) = \sum_{i=1}^{n} (O_i(T) \cdot k_i(S)) $$

Диапазон итогового $Score$: от 0 до 1. (При отображении умножается на 100%).

---

## 5. Проблема "Холодного старта" (Новички на платформе)
Новые репетиторы не имеют отзывов и метрик $O_1, O_2, O_3$. Чтобы они могли получать учеников, применяется:
1. **Базовый "Boost-коэффициент":** Новичкам искусственно присваивается средний рейтинг по платформе + бонус (например, $0.7$ по всем метрикам), который действует на первые 10 просмотров/заявок.
2. **Взвешенная очередь (Explore vs Exploit):** В топ выдачи подмешивается 10-15% новых репетиторов, чтобы собрать для них аналитику и сформировать реальные $O_1..O_4$.

## 6. Периодичность пересчета
*   **Динамические метрики ($O_4$ Отзывчивость):** Пересчитывается раз в сутки или в реальном времени после каждого ответа.
*   **Сложные метрики ($O_1..O_3$):** Пересчитываются асинхронно раз в сутки/неделю (фоновые Job-worker'ы, так как данные меняются только после окончания уроков и выставления оценок).
