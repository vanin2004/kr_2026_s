openapi: "3.1.0"
info:
  title: "TutorApp — Custom Logic API (FastAPI)"
  version: "1.0.0"
  description: |
    Сервис кастомной бизнес-логики на FastAPI.

    Функции:
    - **CRUD-эндпоинты** для всех таблиц
    - **Алгоритм подбора** репетиторов (`/suggestions`)
    - **Внутренние вебхуки** (`/internal/*`) — только для Docker-сети
    - **Health Check** (`/health`)
    - **Push-уведомления** (фоновый воркер через PostgreSQL LISTEN/NOTIFY)

    Через Nginx проксируется по префиксу `/api/custom/`.
    Эндпоинты `/internal/*` не доступны через Nginx наружу.

  contact:
    name: "TutorApp Team"

servers:
  - url: "http://custom-api:8000"
    description: "Внутренняя Docker-сеть (прямой доступ)"
  - url: "http://localhost:80/api/custom"
    description: "Через Nginx Gateway"

paths:
  # ────────────────────────────────────────────────────────────────
  # Health Check
  # ────────────────────────────────────────────────────────────────
  /health:
    get:
      operationId: "healthCheck"
      summary: "Проверка работоспособности сервиса"
      tags: ["Health"]
      responses:
        "200":
          description: "Сервис работает"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "ok"

  # ────────────────────────────────────────────────────────────────
  # Auth (Регистрация)
  # ────────────────────────────────────────────────────────────────
  /api/custom/auth/register:
    post:
      operationId: "registerUser"
      summary: "Регистрация нового пользователя"
      description: |
        Создаёт пользователя одновременно в Keycloak (для аутентификации)
        и в локальной БД (для данных приложения).

        Для репетитора обязательны `subject_id` и `hourly_rate`.
        Для ученика — только username, password, full_name, role.

        Ответ содержит `user_id` — UUID пользователя в системе.
      tags: ["auth"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: "Пользователь успешно зарегистрирован"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterResponse"
        "400":
          description: "Неверные данные (несуществующий subject_id)"
        "409":
          description: "Username уже занят"
        "422":
          description: "Ошибка валидации (не указан subject_id для репетитора и т.п.)"
        "502":
          description: "Сервис регистрации недоступен (Keycloak не отвечает)"

  # ────────────────────────────────────────────────────────────────
  # Suggestions (Подбор репетиторов)
  # ────────────────────────────────────────────────────────────────
  /api/custom/suggestions:
    post:
      operationId: "getSuggestions"
      summary: "Подобрать репетиторов по параметрам"
      description: |
        Запускает двухэтапный алгоритм подбора:

        1. **Hard Filters** (SQL): предмет, макс. цена, минимальный опыт,
           только верифицированные, слоты расписания, обязательные теги.
        2. **Soft Scoring** (Python): расчёт итогового Score = Σ(Oi × ki)
           по пяти метрикам (O1..O5) с весами ученика (k1..k5).

        Результат сортируется по score (убывание).
      tags: ["suggestions"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SuggestionRequest"
      responses:
        "200":
          description: "Отсортированный список репетиторов с оценками"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SuggestionResponse"
        "500":
          description: "Ошибка сервера"

  # ────────────────────────────────────────────────────────────────
  # Internal Webhooks (Keycloak) — только Docker-сеть
  # ────────────────────────────────────────────────────────────────
  /api/custom/internal:
    post:
      operationId: "sendWebhook"
      summary: "Keycloak Event Listener вебхук"
      description: |
        Принимает события от com.vymalo keycloak-webhook-provider.
        Обрабатывает `REGISTER`, `LOGIN`, `LOGOUT`, `UPDATE_PROFILE` и пр.
        **Недоступен через Nginx** — только внутренняя Docker-сеть.
      tags: ["internal"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebhookRequest"
      responses:
        "200":
          description: "Вебхук обработан"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum: [created, already_exists, acknowledged, skipped]
        "400":
          description: "Некорректная роль"
        "422":
          description: "Некорректный UUID"
        "500":
          description: "Внутренняя ошибка"

  /api/custom/internal/sendWebhook:
    post:
      operationId: "sendWebhookAlt"
      summary: "Keycloak вебхук (альтернативный путь /sendWebhook)"
      description: |
        Алиас для POST /api/custom/internal. Некоторые конфигурации
        keycloak-webhook-provider отправляют на /sendWebhook.
        **Недоступен через Nginx** — только внутренняя Docker-сеть.
      tags: ["internal"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebhookRequest"
      responses:
        "200":
          description: "Вебхук обработан"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum: [created, already_exists, acknowledged, skipped]
        "400":
          description: "Некорректная роль"
        "422":
          description: "Некорректный UUID"
        "500":
          description: "Внутренняя ошибка"

  /api/custom/internal/user-created:
    post:
      operationId: "internalUserCreated"
      summary: "Вебхук регистрации пользователя (упрощённый)"
      description: |
        Упрощённый эндпоинт, вызываемый Keycloak Event Listener при событии `REGISTER`.
        Создаёт запись в `users` + пустой профиль (`tutor_profiles` или `student_profiles`).
        **Недоступен через Nginx** — только внутренняя Docker-сеть.
      tags: ["internal"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KeycloakUserCreated"
      responses:
        "200":
          description: "Пользователь создан или уже существует"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum: [created, already_exists]
        "400":
          description: "Некорректная роль"
        "422":
          description: "Некорректный UUID"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Subjects
  # ────────────────────────────────────────────────────────────────
  /api/custom/subjects:
    get:
      operationId: "listSubjects"
      summary: "Список предметов"
      tags: ["subjects"]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
            maximum: 1000
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список предметов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SubjectRead"
    post:
      operationId: "createSubject"
      summary: "Создать предмет"
      tags: ["subjects"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubjectCreate"
      responses:
        "201":
          description: "Предмет создан"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubjectRead"

  /api/custom/subjects/{item_id}:
    get:
      operationId: "getSubject"
      summary: "Получить предмет по ID"
      tags: ["subjects"]
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Данные предмета"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubjectRead"
        "404":
          description: "Предмет не найден"
    patch:
      operationId: "updateSubject"
      summary: "Обновить предмет"
      tags: ["subjects"]
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubjectUpdate"
      responses:
        "200":
          description: "Предмет обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubjectRead"
        "404":
          description: "Предмет не найден"
    delete:
      operationId: "deleteSubject"
      summary: "Удалить предмет"
      tags: ["subjects"]
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Предмет удалён"
        "404":
          description: "Предмет не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Tags
  # ────────────────────────────────────────────────────────────────
  /api/custom/tags:
    get:
      operationId: "listTags"
      summary: "Список тегов"
      tags: ["tags"]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
            maximum: 1000
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список тегов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TagRead"
    post:
      operationId: "createTag"
      summary: "Создать тег"
      tags: ["tags"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagCreate"
      responses:
        "201":
          description: "Тег создан"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagRead"

  /api/custom/tags/{item_id}:
    get:
      operationId: "getTag"
      summary: "Получить тег по ID"
      tags: ["tags"]
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Данные тега"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagRead"
        "404":
          description: "Тег не найден"
    patch:
      operationId: "updateTag"
      summary: "Обновить тег"
      tags: ["tags"]
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagUpdate"
      responses:
        "200":
          description: "Тег обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagRead"
        "404":
          description: "Тег не найден"
    delete:
      operationId: "deleteTag"
      summary: "Удалить тег"
      tags: ["tags"]
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Тег удалён"
        "404":
          description: "Тег не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Users
  # ────────────────────────────────────────────────────────────────
  /api/custom/users:
    get:
      operationId: "listUsers"
      summary: "Список пользователей"
      tags: ["users"]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список пользователей"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserRead"

  /api/custom/users/{user_id}:
    get:
      operationId: "getUser"
      summary: "Получить пользователя по UUID"
      tags: ["users"]
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Данные пользователя"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserRead"
        "404":
          description: "Пользователь не найден"
    delete:
      operationId: "deleteUser"
      summary: "Удалить пользователя"
      tags: ["users"]
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Пользователь удалён"
        "404":
          description: "Пользователь не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Tutor Profiles
  # ────────────────────────────────────────────────────────────────
  /api/custom/tutor_profiles:
    get:
      operationId: "listTutorProfiles"
      summary: "Список профилей репетиторов"
      tags: ["tutor_profiles"]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: subject_id
          in: query
          schema:
            type: string
            format: uuid
          description: "Фильтр по предмету"
        - name: hourly_rate_lte
          in: query
          schema:
            type: integer
          description: "Фильтр: макс. цена (hourly_rate <= значение)"
      responses:
        "200":
          description: "Список профилей"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TutorProfileRead"
    post:
      operationId: "createTutorProfile"
      summary: "Создать профиль репетитора"
      tags: ["tutor_profiles"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TutorProfileCreate"
      responses:
        "201":
          description: "Профиль создан"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TutorProfileRead"

  /api/custom/tutor_profiles/{user_id}:
    get:
      operationId: "getTutorProfile"
      summary: "Получить профиль репетитора"
      tags: ["tutor_profiles"]
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Профиль репетитора"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TutorProfileRead"
        "404":
          description: "Профиль не найден"
    patch:
      operationId: "updateTutorProfile"
      summary: "Обновить профиль репетитора"
      tags: ["tutor_profiles"]
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TutorProfileUpdate"
      responses:
        "200":
          description: "Профиль обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TutorProfileRead"
        "404":
          description: "Профиль не найден"
    delete:
      operationId: "deleteTutorProfile"
      summary: "Удалить профиль репетитора"
      tags: ["tutor_profiles"]
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Профиль удалён"
        "404":
          description: "Профиль не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Student Profiles
  # ────────────────────────────────────────────────────────────────
  /api/custom/student_profiles:
    get:
      operationId: "listStudentProfiles"
      summary: "Список профилей учеников"
      tags: ["student_profiles"]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
            maximum: 1000
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список профилей учеников"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StudentProfileRead"

  /api/custom/student_profiles/{user_id}:
    get:
      operationId: "getStudentProfile"
      summary: "Получить профиль ученика"
      tags: ["student_profiles"]
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Профиль ученика"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentProfileRead"
        "404":
          description: "Профиль не найден"
    post:
      operationId: "createStudentProfile"
      summary: "Создать профиль ученика"
      tags: ["student_profiles"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StudentProfileCreate"
      responses:
        "201":
          description: "Профиль создан"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentProfileRead"
    patch:
      operationId: "updateStudentProfile"
      summary: "Обновить профиль ученика"
      tags: ["student_profiles"]
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StudentProfileUpdate"
      responses:
        "200":
          description: "Профиль обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentProfileRead"
        "404":
          description: "Профиль не найден"
    delete:
      operationId: "deleteStudentProfile"
      summary: "Удалить профиль ученика"
      tags: ["student_profiles"]
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Профиль удалён"
        "404":
          description: "Профиль не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Tutor Tags
  # ────────────────────────────────────────────────────────────────
  /api/custom/tutor_tags:
    get:
      operationId: "listTutorTags"
      summary: "Список тегов репетиторов"
      tags: ["tutor_tags"]
      parameters:
        - name: tutor_id
          in: query
          schema:
            type: string
            format: uuid
        - name: tag_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Список тегов репетиторов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TutorTagRead"
    post:
      operationId: "createTutorTag"
      summary: "Добавить тег репетитору"
      tags: ["tutor_tags"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TutorTagCreate"
      responses:
        "201":
          description: "Тег добавлен"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TutorTagRead"
    delete:
      operationId: "deleteTutorTag"
      summary: "Удалить тег у репетитора"
      tags: ["tutor_tags"]
      parameters:
        - name: tutor_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: tag_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Тег удалён"
        "404":
          description: "Тег не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Student Preferred Tags
  # ────────────────────────────────────────────────────────────────
  /api/custom/student_preferred_tags:
    get:
      operationId: "listStudentPreferredTags"
      summary: "Список желаемых тегов ученика"
      tags: ["student_preferred_tags"]
      parameters:
        - name: student_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Список тегов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StudentPreferredTagRead"
    post:
      operationId: "createStudentPreferredTag"
      summary: "Добавить желаемый тег ученику"
      tags: ["student_preferred_tags"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StudentPreferredTagCreate"
      responses:
        "201":
          description: "Тег добавлен"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentPreferredTagRead"
    delete:
      operationId: "deleteStudentPreferredTag"
      summary: "Удалить желаемый тег ученика"
      tags: ["student_preferred_tags"]
      parameters:
        - name: student_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: tag_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Тег удалён"
        "404":
          description: "Тег не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Tutor Certifications
  # ────────────────────────────────────────────────────────────────
  /api/custom/tutor_certifications:
    get:
      operationId: "listTutorCertifications"
      summary: "Список сертификатов репетитора"
      tags: ["tutor_certifications"]
      parameters:
        - name: tutor_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Список сертификатов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TutorCertificationRead"
    post:
      operationId: "createTutorCertification"
      summary: "Добавить сертификат репетитору"
      tags: ["tutor_certifications"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TutorCertificationCreate"
      responses:
        "201":
          description: "Сертификат добавлен"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TutorCertificationRead"

  /api/custom/tutor_certifications/{cert_id}:
    get:
      operationId: "getTutorCertification"
      summary: "Получить сертификат репетитора по ID"
      tags: ["tutor_certifications"]
      parameters:
        - name: cert_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: "Сертификат"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TutorCertificationRead"
        "404":
          description: "Сертификат не найден"
    patch:
      operationId: "updateTutorCertification"
      summary: "Обновить сертификат (подтвердить и т.п.)"
      tags: ["tutor_certifications"]
      parameters:
        - name: cert_id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TutorCertificationUpdate"
      responses:
        "200":
          description: "Сертификат обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TutorCertificationRead"
        "404":
          description: "Сертификат не найден"
    delete:
      operationId: "deleteTutorCertification"
      summary: "Удалить сертификат репетитора"
      tags: ["tutor_certifications"]
      parameters:
        - name: cert_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: "Сертификат удалён"
        "404":
          description: "Сертификат не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Schedules
  # ────────────────────────────────────────────────────────────────
  /api/custom/schedules:
    get:
      operationId: "listSchedules"
      summary: "Список слотов расписания"
      tags: ["schedules"]
      parameters:
        - name: tutor_id
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список слотов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ScheduleRead"
    post:
      operationId: "createSchedule"
      summary: "Добавить слот расписания"
      tags: ["schedules"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduleCreate"
      responses:
        "201":
          description: "Слот создан"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleRead"

  /api/custom/schedules/{schedule_id}:
    get:
      operationId: "getSchedule"
      summary: "Получить слот расписания"
      tags: ["schedules"]
      parameters:
        - name: schedule_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: "Слот расписания"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleRead"
        "404":
          description: "Слот не найден"
    patch:
      operationId: "updateSchedule"
      summary: "Изменить слот расписания"
      tags: ["schedules"]
      parameters:
        - name: schedule_id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduleUpdate"
      responses:
        "200":
          description: "Слот обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleRead"
        "404":
          description: "Слот не найден"
    delete:
      operationId: "deleteSchedule"
      summary: "Удалить слот расписания"
      tags: ["schedules"]
      parameters:
        - name: schedule_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: "Слот удалён"
        "404":
          description: "Слот не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Lessons
  # ────────────────────────────────────────────────────────────────
  /api/custom/lessons:
    get:
      operationId: "listLessons"
      summary: "Список уроков"
      tags: ["lessons"]
      parameters:
        - name: student_id
          in: query
          schema:
            type: string
            format: uuid
        - name: tutor_id
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список уроков"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LessonRead"
    post:
      operationId: "createLesson"
      summary: "Забронировать урок"
      tags: ["lessons"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LessonCreate"
      responses:
        "201":
          description: "Урок создан"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonRead"

  /api/custom/lessons/{lesson_id}:
    get:
      operationId: "getLesson"
      summary: "Получить урок по ID"
      tags: ["lessons"]
      parameters:
        - name: lesson_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Данные урока"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonRead"
        "404":
          description: "Урок не найден"
    patch:
      operationId: "updateLesson"
      summary: "Обновить урок (подтвердить, отменить, добавить ссылку)"
      tags: ["lessons"]
      parameters:
        - name: lesson_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LessonUpdate"
      responses:
        "200":
          description: "Урок обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonRead"
        "404":
          description: "Урок не найден"
    delete:
      operationId: "deleteLesson"
      summary: "Удалить урок"
      tags: ["lessons"]
      parameters:
        - name: lesson_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Урок удалён"
        "404":
          description: "Урок не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Applications
  # ────────────────────────────────────────────────────────────────
  /api/custom/applications:
    get:
      operationId: "listApplications"
      summary: "Список заявок"
      tags: ["applications"]
      parameters:
        - name: student_id
          in: query
          schema:
            type: string
            format: uuid
        - name: tutor_id
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список заявок"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ApplicationRead"
    post:
      operationId: "createApplication"
      summary: "Подать заявку репетитору"
      tags: ["applications"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationCreate"
      responses:
        "201":
          description: "Заявка создана"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationRead"

  /api/custom/applications/{application_id}:
    get:
      operationId: "getApplication"
      summary: "Получить заявку по ID"
      tags: ["applications"]
      parameters:
        - name: application_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Данные заявки"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationRead"
        "404":
          description: "Заявка не найдена"
    patch:
      operationId: "updateApplication"
      summary: "Изменить статус заявки (принять / отклонить)"
      description: |
        Репетитор может принять (`accepted`) или отклонить (`rejected`) заявку.
        При принятии автоматически создаётся чат.
        При любом изменении статуса заполняется `responded_at`.
      tags: ["applications"]
      parameters:
        - name: application_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationUpdate"
      responses:
        "200":
          description: "Заявка обновлена"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationRead"
        "404":
          description: "Заявка не найдена"
    delete:
      operationId: "deleteApplication"
      summary: "Удалить заявку"
      tags: ["applications"]
      parameters:
        - name: application_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Заявка удалена"
        "404":
          description: "Заявка не найдена"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Chats
  # ────────────────────────────────────────────────────────────────
  /api/custom/chats:
    get:
      operationId: "listChats"
      summary: "Список чатов"
      tags: ["chats"]
      parameters:
        - name: application_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Список чатов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatRead"

  /api/custom/chats/{chat_id}:
    get:
      operationId: "getChat"
      summary: "Получить чат по ID"
      tags: ["chats"]
      parameters:
        - name: chat_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Чат"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatRead"
        "404":
          description: "Чат не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Messages
  # ────────────────────────────────────────────────────────────────
  /api/custom/messages:
    get:
      operationId: "listMessages"
      summary: "История сообщений в чате"
      tags: ["messages"]
      parameters:
        - name: chat_id
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список сообщений"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MessageRead"
    post:
      operationId: "createMessage"
      summary: "Отправить сообщение"
      tags: ["messages"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageCreate"
      responses:
        "201":
          description: "Сообщение отправлено"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageRead"

  /api/custom/messages/{message_id}:
    patch:
      operationId: "updateMessage"
      summary: "Обновить сообщение (например, отметить прочитанным)"
      tags: ["messages"]
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageUpdate"
      responses:
        "200":
          description: "Сообщение обновлено"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageRead"
        "404":
          description: "Сообщение не найдено"
    get:
      operationId: "getMessage"
      summary: "Получить сообщение по ID"
      tags: ["messages"]
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Сообщение"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageRead"
        "404":
          description: "Сообщение не найдено"
    delete:
      operationId: "deleteMessage"
      summary: "Удалить сообщение"
      tags: ["messages"]
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Сообщение удалено"
        "404":
          description: "Сообщение не найдено"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Test Library
  # ────────────────────────────────────────────────────────────────
  /api/custom/test_library:
    get:
      operationId: "listTestLibrary"
      summary: "Список тестов"
      tags: ["test_library"]
      parameters:
        - name: subject_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Список тестов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TestLibraryRead"
    post:
      operationId: "createTestLibrary"
      summary: "Добавить тест в библиотеку"
      tags: ["test_library"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestLibraryCreate"
      responses:
        "201":
          description: "Тест создан"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestLibraryRead"

  /api/custom/test_library/{test_id}:
    get:
      operationId: "getTestLibrary"
      summary: "Получить тест по ID"
      tags: ["test_library"]
      parameters:
        - name: test_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Тест"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestLibraryRead"
        "404":
          description: "Тест не найден"
    patch:
      operationId: "updateTestLibrary"
      summary: "Обновить тест"
      tags: ["test_library"]
      parameters:
        - name: test_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestLibraryUpdate"
      responses:
        "200":
          description: "Тест обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestLibraryRead"
        "404":
          description: "Тест не найден"
    delete:
      operationId: "deleteTestLibrary"
      summary: "Удалить тест"
      tags: ["test_library"]
      parameters:
        - name: test_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Тест удалён"
        "404":
          description: "Тест не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Student Results
  # ────────────────────────────────────────────────────────────────
  /api/custom/student_results:
    get:
      operationId: "listStudentResults"
      summary: "Список результатов тестирования"
      tags: ["student_results"]
      parameters:
        - name: student_id
          in: query
          schema:
            type: string
            format: uuid
        - name: tutor_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Список результатов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StudentResultRead"
    post:
      operationId: "createStudentResult"
      summary: "Назначить тест ученику"
      tags: ["student_results"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StudentResultCreate"
      responses:
        "201":
          description: "Результат создан (тест назначен)"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentResultRead"

  /api/custom/student_results/{result_id}:
    patch:
      operationId: "updateStudentResult"
      summary: "Обновить результат (сдать тест)"
      description: |
        Ученик заполняет `score` и `completed_at` при сдаче теста.
      tags: ["student_results"]
      parameters:
        - name: result_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StudentResultUpdate"
      responses:
        "200":
          description: "Результат обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentResultRead"
        "404":
          description: "Результат не найден"
    get:
      operationId: "getStudentResult"
      summary: "Получить результат тестирования по ID"
      tags: ["student_results"]
      parameters:
        - name: result_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Результат"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentResultRead"
        "404":
          description: "Результат не найден"
    delete:
      operationId: "deleteStudentResult"
      summary: "Удалить результат тестирования"
      tags: ["student_results"]
      parameters:
        - name: result_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Результат удалён"
        "404":
          description: "Результат не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Reviews
  # ────────────────────────────────────────────────────────────────
  /api/custom/reviews:
    get:
      operationId: "listReviews"
      summary: "Список отзывов"
      tags: ["reviews"]
      parameters:
        - name: tutor_id
          in: query
          schema:
            type: string
            format: uuid
        - name: student_id
          in: query
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: "Список отзывов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReviewRead"
    post:
      operationId: "createReview"
      summary: "Оставить отзыв на репетитора"
      tags: ["reviews"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewCreate"
      responses:
        "201":
          description: "Отзыв создан"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewRead"

  /api/custom/reviews/{review_id}:
    get:
      operationId: "getReview"
      summary: "Получить отзыв по ID"
      tags: ["reviews"]
      parameters:
        - name: review_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Отзыв"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewRead"
        "404":
          description: "Отзыв не найден"
    patch:
      operationId: "updateReview"
      summary: "Обновить отзыв"
      tags: ["reviews"]
      parameters:
        - name: review_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewUpdate"
      responses:
        "200":
          description: "Отзыв обновлён"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewRead"
        "404":
          description: "Отзыв не найден"
    delete:
      operationId: "deleteReview"
      summary: "Удалить отзыв"
      tags: ["reviews"]
      parameters:
        - name: review_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: "Отзыв удалён"
        "404":
          description: "Отзыв не найден"

  # ────────────────────────────────────────────────────────────────
  # CRUD — Device Tokens
  # ────────────────────────────────────────────────────────────────
  /api/custom/device_tokens:
    get:
      operationId: "listDeviceTokens"
      summary: "Список FCM-токенов устройства"
      tags: ["device_tokens"]
      parameters:
        - name: user_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: "Список токенов"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DeviceTokenRead"
    post:
      operationId: "createDeviceToken"
      summary: "Зарегистрировать FCM-токен устройства"
      tags: ["device_tokens"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeviceTokenCreate"
      responses:
        "201":
          description: "Токен зарегистрирован"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceTokenRead"

  /api/custom/device_tokens/{token_id}:
    delete:
      operationId: "deleteDeviceToken"
      summary: "Удалить FCM-токен устройства"
      tags: ["device_tokens"]
      parameters:
        - name: token_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: "Токен удалён"
        "404":
          description: "Токен не найден"
    get:
      operationId: "getDeviceToken"
      summary: "Получить FCM-токен по ID"
      tags: ["device_tokens"]
      parameters:
        - name: token_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: "Токен"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceTokenRead"
        "404":
          description: "Токен не найден"

components:
  schemas:
    # ─── Auth (Регистрация) ─────────────────────────────────────
    RegisterRequest:
      type: object
      required: [username, password, full_name, role]
      properties:
        username:
          type: string
          description: "Логин пользователя"
          example: "ivan.petrov"
        password:
          type: string
          description: "Пароль"
          example: "qwerty123"
        full_name:
          type: string
          description: "ФИО"
          example: "Петров Иван Сергеевич"
        role:
          type: string
          enum: ["tutor", "student"]
          description: "Тип учётной записи"
        subject_id:
          type: string
          format: uuid
          description: "ID предмета (обязательно для tutor)"
        hourly_rate:
          type: integer
          description: "Стоимость часа в рублях (обязательно для tutor)"
          example: 1500
    RegisterResponse:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
          description: "UUID пользователя в системе"
        username:
          type: string
          description: "Логин"
        role:
          type: string
          enum: ["tutor", "student"]
          description: "Тип учётной записи"

    # ─── Subjects ─────────────────────────────────────────────────
    SubjectCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 100
          example: "Математика"
    SubjectRead:
      allOf:
        - $ref: "#/components/schemas/SubjectCreate"
        - type: object
          properties:
            id:
              type: string
              format: uuid
    SubjectUpdate:
      type: object
      properties:
        name:
          type: string
          maxLength: 100

    # ─── Tags ─────────────────────────────────────────────────────
    TagCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 100
          example: "#подготовка_к_ЕГЭ"
    TagRead:
      allOf:
        - $ref: "#/components/schemas/TagCreate"
        - type: object
          properties:
            id:
              type: string
              format: uuid
    TagUpdate:
      type: object
      properties:
        name:
          type: string
          maxLength: 100

    # ─── Users ────────────────────────────────────────────────────
    UserRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        role:
          type: string
          enum: [tutor, student]
        created_at:
          type: string
          format: date-time

    # ─── Tutor Profile ────────────────────────────────────────────
    TutorProfileCreate:
      type: object
      required: [user_id]
      properties:
        user_id:
          type: string
          format: uuid
        full_name:
          type: string
          example: "Иван Петров"
        photo_url:
          type: string
          format: uri
        education:
          type: string
          example: "МГУ, механико-математический факультет"
        subject_id:
          type: string
          format: uuid
        hourly_rate:
          type: integer
          description: "Цена в копейках (RUB)"
          example: 120000
        experience_years:
          type: integer
          default: 0
        is_verified:
          type: boolean
          default: false
        student_count:
          type: integer
          default: 0
    TutorProfileRead:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        full_name:
          type: string
        photo_url:
          type: string
          nullable: true
        education:
          type: string
          nullable: true
        subject_id:
          type: string
          format: uuid
          nullable: true
        hourly_rate:
          type: integer
          nullable: true
        experience_years:
          type: integer
        is_verified:
          type: boolean
        student_count:
          type: integer
        rating_efficiency:
          type: number
          format: decimal
          nullable: true
        rating_communication:
          type: number
          format: decimal
          nullable: true
        rating_expertise:
          type: number
          format: decimal
          nullable: true
        rating_responsiveness:
          type: number
          format: decimal
          nullable: true
        is_new_boost:
          type: boolean
    TutorProfileUpdate:
      type: object
      properties:
        full_name:
          type: string
        photo_url:
          type: string
        education:
          type: string
        subject_id:
          type: string
          format: uuid
        hourly_rate:
          type: integer
        experience_years:
          type: integer
        is_verified:
          type: boolean
        is_new_boost:
          type: boolean

    # ─── Student Profile ──────────────────────────────────────────
    StudentProfileCreate:
      type: object
      required: [user_id]
      properties:
        user_id:
          type: string
          format: uuid
        full_name:
          type: string
        photo_url:
          type: string
        search_weights:
          type: object
          description: "Веса алгоритма подбора (k1..k5)"
          properties:
            k1_effectiveness:
              type: number
              default: 0.30
            k2_communication:
              type: number
              default: 0.15
            k3_expertise:
              type: number
              default: 0.20
            k4_responsiveness:
              type: number
              default: 0.15
            k5_tags:
              type: number
              default: 0.20
    StudentProfileRead:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        full_name:
          type: string
          nullable: true
        photo_url:
          type: string
          nullable: true
        search_weights:
          type: object
          nullable: true
    StudentProfileUpdate:
      type: object
      properties:
        full_name:
          type: string
        photo_url:
          type: string
        search_weights:
          type: object

    # ─── Tutor Tag ────────────────────────────────────────────────
    TutorTagCreate:
      type: object
      required: [tutor_id, tag_id]
      properties:
        tutor_id:
          type: string
          format: uuid
        tag_id:
          type: string
          format: uuid
    TutorTagRead:
      type: object
      properties:
        tutor_id:
          type: string
          format: uuid
        tag_id:
          type: string
          format: uuid

    # ─── Student Preferred Tag ────────────────────────────────────
    StudentPreferredTagCreate:
      type: object
      required: [student_id, tag_id]
      properties:
        student_id:
          type: string
          format: uuid
        tag_id:
          type: string
          format: uuid
        is_required:
          type: boolean
          default: false
          description: "true = жёсткий фильтр, false = мягкая оценка O5"
    StudentPreferredTagRead:
      type: object
      properties:
        student_id:
          type: string
          format: uuid
        tag_id:
          type: string
          format: uuid
        is_required:
          type: boolean

    # ─── Tutor Certification ──────────────────────────────────────
    TutorCertificationCreate:
      type: object
      required: [tutor_id, title, file_url]
      properties:
        tutor_id:
          type: string
          format: uuid
        title:
          type: string
          maxLength: 255
          example: "Диплом МГУ"
        file_url:
          type: string
          format: uri
          maxLength: 500
    TutorCertificationRead:
      type: object
      properties:
        id:
          type: integer
        tutor_id:
          type: string
          format: uuid
        title:
          type: string
        file_url:
          type: string
        is_verified:
          type: boolean
        created_at:
          type: string
          format: date-time

    TutorCertificationUpdate:
      type: object
      properties:
        title:
          type: string
          maxLength: 255
        file_url:
          type: string
          format: uri
          maxLength: 500
        is_verified:
          type: boolean

    # ─── Schedule ─────────────────────────────────────────────────
    ScheduleCreate:
      type: object
      required: [tutor_id, start_time, end_time]
      properties:
        tutor_id:
          type: string
          format: uuid
        day_of_week:
          type: integer
          minimum: 1
          maximum: 7
          description: "1=Пн ... 7=Вс (NULL для разового слота)"
        specific_date:
          type: string
          format: date
          description: "Дата разового слота (NULL для регулярного)"
        start_time:
          type: string
          format: time
          example: "18:00:00"
        end_time:
          type: string
          format: time
          example: "20:00:00"
    ScheduleRead:
      type: object
      properties:
        id:
          type: integer
        tutor_id:
          type: string
          format: uuid
        day_of_week:
          type: integer
          nullable: true
        specific_date:
          type: string
          format: date
          nullable: true
        start_time:
          type: string
          format: time
        end_time:
          type: string
          format: time
    ScheduleUpdate:
      type: object
      properties:
        day_of_week:
          type: integer
          minimum: 1
          maximum: 7
          nullable: true
        specific_date:
          type: string
          format: date
          nullable: true
        start_time:
          type: string
          format: time
        end_time:
          type: string
          format: time

    # ─── Lesson ───────────────────────────────────────────────────
    LessonCreate:
      type: object
      required: [student_id, tutor_id, start_datetime, end_datetime]
      properties:
        student_id:
          type: string
          format: uuid
        tutor_id:
          type: string
          format: uuid
        start_datetime:
          type: string
          format: date-time
        end_datetime:
          type: string
          format: date-time
        meeting_link:
          type: string
          format: uri
    LessonRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
        student_id:
          type: string
          format: uuid
        tutor_id:
          type: string
          format: uuid
        start_datetime:
          type: string
          format: date-time
        end_datetime:
          type: string
          format: date-time
        status:
          type: string
          enum: [planned, completed, cancelled]
        meeting_link:
          type: string
          nullable: true
    LessonUpdate:
      type: object
      properties:
        status:
          type: string
          enum: [planned, completed, cancelled]
        meeting_link:
          type: string

    # ─── Application ──────────────────────────────────────────────
    ApplicationCreate:
      type: object
      required: [student_id, tutor_id]
      properties:
        student_id:
          type: string
          format: uuid
        tutor_id:
          type: string
          format: uuid
    ApplicationRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
        student_id:
          type: string
          format: uuid
        tutor_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending, accepted, rejected]
        created_at:
          type: string
          format: date-time
        responded_at:
          type: string
          format: date-time
          nullable: true
    ApplicationUpdate:
      type: object
      properties:
        status:
          type: string
          enum: [pending, accepted, rejected]

    # ─── Chat ─────────────────────────────────────────────────────
    ChatRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
        application_id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    # ─── Message ──────────────────────────────────────────────────
    MessageCreate:
      type: object
      required: [chat_id, sender_id, text]
      properties:
        chat_id:
          type: string
          format: uuid
        sender_id:
          type: string
          format: uuid
        text:
          type: string
    MessageRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
        chat_id:
          type: string
          format: uuid
        sender_id:
          type: string
          format: uuid
        text:
          type: string
        created_at:
          type: string
          format: date-time
        is_read:
          type: boolean
    MessageUpdate:
      type: object
      properties:
        is_read:
          type: boolean

    # ─── Test Library ─────────────────────────────────────────────
    TestLibraryCreate:
      type: object
      required: [subject_id, topic, questions_json]
      properties:
        subject_id:
          type: string
          format: uuid
        topic:
          type: string
          maxLength: 255
        questions_json:
          type: object
          description: "Вопросы теста в JSON-формате"
    TestLibraryRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
        subject_id:
          type: string
          format: uuid
        topic:
          type: string
        questions_json:
          type: object

    TestLibraryUpdate:
      type: object
      properties:
        topic:
          type: string
          maxLength: 255
        questions_json:
          type: object

    # ─── Student Result ───────────────────────────────────────────
    StudentResultCreate:
      type: object
      required: [student_id, tutor_id, test_id, type]
      properties:
        student_id:
          type: string
          format: uuid
        tutor_id:
          type: string
          format: uuid
        test_id:
          type: string
          format: uuid
        type:
          type: string
          enum: [initial_test, control_test]
    StudentResultRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
        student_id:
          type: string
          format: uuid
        tutor_id:
          type: string
          format: uuid
        test_id:
          type: string
          format: uuid
        type:
          type: string
        score:
          type: number
          nullable: true
        assigned_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
          nullable: true
    StudentResultUpdate:
      type: object
      properties:
        score:
          type: number
        completed_at:
          type: string
          format: date-time

    # ─── Review ───────────────────────────────────────────────────
    ReviewCreate:
      type: object
      required: [student_id, tutor_id, communication_score]
      properties:
        student_id:
          type: string
          format: uuid
        tutor_id:
          type: string
          format: uuid
        lesson_id:
          type: string
          format: uuid
        communication_score:
          type: integer
          minimum: 1
          maximum: 5
        text:
          type: string
    ReviewRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
        student_id:
          type: string
          format: uuid
        tutor_id:
          type: string
          format: uuid
        lesson_id:
          type: string
          format: uuid
          nullable: true
        communication_score:
          type: integer
        text:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time

    ReviewUpdate:
      type: object
      properties:
        communication_score:
          type: integer
          minimum: 1
          maximum: 5
        text:
          type: string

    # ─── Device Token ─────────────────────────────────────────────
    DeviceTokenCreate:
      type: object
      required: [user_id, token, platform]
      properties:
        user_id:
          type: string
          format: uuid
        token:
          type: string
          maxLength: 500
          description: "FCM-токен устройства"
        platform:
          type: string
          enum: [android, ios]
    DeviceTokenRead:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: string
          format: uuid
        token:
          type: string
        platform:
          type: string
        updated_at:
          type: string
          format: date-time

    # ─── Suggestions ──────────────────────────────────────────────
    ScheduleSlot:
      type: object
      required: [day_of_week, start_time, end_time]
      properties:
        day_of_week:
          type: integer
          minimum: 1
          maximum: 7
          description: "1=Пн, 7=Вс"
        start_time:
          type: string
          format: time
          example: "18:00:00"
        end_time:
          type: string
          format: time
          example: "20:00:00"
    SuggestionWeights:
      type: object
      properties:
        k1_effectiveness:
          type: number
          default: 0.30
          minimum: 0.0
          maximum: 1.0
          description: "Вес эффективности (O1)"
        k2_communication:
          type: number
          default: 0.15
          minimum: 0.0
          maximum: 1.0
          description: "Вес коммуникации (O2)"
        k3_expertise:
          type: number
          default: 0.20
          minimum: 0.0
          maximum: 1.0
          description: "Вес экспертизы (O3)"
        k4_responsiveness:
          type: number
          default: 0.15
          minimum: 0.0
          maximum: 1.0
          description: "Вес отзывчивости (O4)"
        k5_tags:
          type: number
          default: 0.20
          minimum: 0.0
          maximum: 1.0
          description: "Вес совпадения тегов (O5)"
    SuggestionRequest:
      type: object
      required: [subject_id]
      properties:
        subject_id:
          type: string
          format: uuid
          description: "ID предмета"
        max_price:
          type: integer
          description: "Максимальная цена (в копейках)"
        min_experience:
          type: integer
          default: 0
          minimum: 0
          description: "Минимальный стаж (лет)"
        verified_only:
          type: boolean
          default: false
          description: "Только верифицированные репетиторы"
        schedule_slots:
          type: array
          items:
            $ref: "#/components/schemas/ScheduleSlot"
          description: "Совместимость по расписанию"
        required_tag_ids:
          type: array
          items:
            type: string
            format: uuid
          description: "Обязательные теги (используются в O5)"
        weights:
          $ref: "#/components/schemas/SuggestionWeights"
    ScoreBreakdown:
      type: object
      properties:
        o1:
          type: number
          description: "Effectiveness (эффективность)"
        o2:
          type: number
          description: "Communication (коммуникация)"
        o3:
          type: number
          description: "Expertise (экспертиза)"
        o4:
          type: number
          description: "Responsiveness (отзывчивость)"
        o5:
          type: number
          description: "Tag Match (совпадение тегов)"
    SuggestionResponse:
      type: object
      properties:
        tutor_id:
          type: string
          format: uuid
        full_name:
          type: string
          nullable: true
        score:
          type: number
          description: "Итоговая оценка [0..1]"
        score_breakdown:
          $ref: "#/components/schemas/ScoreBreakdown"
        hourly_rate:
          type: integer
          nullable: true
          description: "Цена в копейках"
        is_new:
          type: boolean
          description: "Cold-start boost активен"

    # ─── Keycloak Webhooks ─────────────────────────────────────────
    WebhookRequest:
      type: object
      required: [type]
      properties:
        type:
          type: string
          description: "Тип события (REGISTER, LOGIN, LOGOUT, UPDATE_PROFILE и др.)"
        realmId:
          type: string
          description: "ID realm'а"
        id:
          type: string
          description: "ID события"
        time:
          type: number
          description: "Время события"
        clientId:
          type: string
          description: "ID клиента"
        userId:
          type: string
          description: "UUID пользователя из Keycloak"
        ipAddress:
          type: string
          description: "IP-адрес"
        error:
          type: string
          description: "Ошибка (если есть)"
        details:
          type: object
          description: "Детали события (email, realm_role и пр.)"
        resourcePath:
          type: string
          description: "Путь к ресурсу"
        representation:
          type: string
          description: "JSON-представление"

    KeycloakUserCreated:
      type: object
      required: [userId, email, realmRole]
      properties:
        userId:
          type: string
          description: "UUID пользователя из Keycloak"
        email:
          type: string
          format: email
        realmRole:
          type: string
          enum: [tutor, student]
          description: "Роль пользователя"

tags:
  - name: "auth"
    description: "Регистрация пользователей"
  - name: "Health"
    description: "Health Check"
  - name: "suggestions"
    description: "Подбор репетиторов"
  - name: "internal"
    description: "Внутренние вебхуки (только Docker-сеть)"
  - name: "subjects"
    description: "Справочник предметов"
  - name: "tags"
    description: "Справочник тегов"
  - name: "users"
    description: "Пользователи"
  - name: "tutor_profiles"
    description: "Профили репетиторов"
  - name: "student_profiles"
    description: "Профили учеников"
  - name: "tutor_tags"
    description: "Теги репетиторов"
  - name: "student_preferred_tags"
    description: "Желаемые теги учеников"
  - name: "tutor_certifications"
    description: "Сертификаты репетиторов"
  - name: "schedules"
    description: "Расписание репетиторов"
  - name: "lessons"
    description: "Уроки"
  - name: "applications"
    description: "Заявки на занятия"
  - name: "chats"
    description: "Чаты"
  - name: "messages"
    description: "Сообщения"
  - name: "test_library"
    description: "Библиотека тестов"
  - name: "student_results"
    description: "Результаты тестирования"
  - name: "reviews"
    description: "Отзывы"
  - name: "device_tokens"
    description: "FCM-токены устройств"
